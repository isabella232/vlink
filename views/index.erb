<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="/skeleton.css" />
  <title>vLink</title>
</head>

<body>
<div class="container">
<div>
<h2><strong>vLink - Create simple, easy-to-remember links</strong></h2>
</div>

<% if !@error_messages.nil? %>
  <div class="error">
    <%= "ERRORS: #{@error_messages}" %>
  </div>
<% end %>

<div>
<h3>Create New Link</h3>
<%= form('/', :post) %>
<% fieldset(:link) do |f| %>
  <%= f.input(:name, {:value => @link_name }) %>
  <%= f.input(:target, {:value => @link_target}) %>
<% end %>
<%= submit %>
</div>

<div>
<h3>Current Links</h3>
<table class="u-full-width">
<thead>
  <tr>
    <th>Name</th>
    <th>Target</th>
  </tr>
</thead>
  <% @links.each do |link| %>
    <tr>
      <td><%= link.name %> </td>
      <td><%= "<a href=#{link.target}>#{link.target}</a>" %></td>
    </tr>
  <% end %>
</table>
</div>
</body>
</div>
